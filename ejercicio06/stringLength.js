//Creamos la funcion medir que recibirá por parámetros un string

function medir(palabra) {
  //la función devuelve la longitud del string que recibe
  return palabra.length;
}

//Exportamos la función medir
module.exports = medir;
